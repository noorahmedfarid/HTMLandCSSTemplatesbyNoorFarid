create a CSS theme for a website using different CSS properties that control color, borders, and backgrounds

use different CSS color value types (Hex, RGB, RGBA, and HSL) to define colors

create a dark theme using hsl() and the CSS invert filter

and customize a theme using CSS hooks